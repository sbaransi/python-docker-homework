apiVersion: v1
kind: Service
metadata:
  name: flask-monitor-service
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5001
      protocol: TCP
  selector:
    app: flask-monitor